# Core (V1 MVP)
pandas>=2.0
numpy>=1.24
yfinance>=0.2.40
streamlit>=1.30
python-dotenv>=1.0

# AI providers (optional — system runs offline without these)
anthropic>=0.40
openai>=1.40

# Dev / test
pytest>=8.0
pytest-cov>=5.0
